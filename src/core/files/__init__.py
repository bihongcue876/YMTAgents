"""内置文件访问层（v0.0.11 切片 H/A）。

- paths：路径规范化、禁止区与排除清单（单一来源，D-5）；
- ops：读 / 查 / 写 / 改的有界实现（stdlib，剪枝 + 流式 + 行尾保持）；
- manager：file.* 工具面的装配与调用分派（切片 A 起）。

门禁（D-12）：本包**不 import core.workspace**；需要「当前工作区 root / 数据根」时由 app 层注入。
"""
